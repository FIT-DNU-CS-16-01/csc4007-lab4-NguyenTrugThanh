# Notebooks

Thư mục này dành cho notebook khám phá thêm.

Khuyến nghị:

- Không để toàn bộ lời giải chỉ nằm trong notebook.
- Code chính nên nằm trong `src/` và được chạy qua `run_lab4.py`.
- Notebook chỉ nên dùng để đọc output, vẽ thêm biểu đồ, hoặc phân tích lỗi.
