# Data folder

`sample_imdb_tiny.csv` chỉ dùng cho smoke test, CI và kiểm tra nhanh pipeline.

Khi làm bài chính thức, sinh viên nên chạy với:

```bash
python run_lab4.py --dataset imdb
```

Không nên dùng kết quả từ file tiny CSV để kết luận mô hình tốt hay kém.
